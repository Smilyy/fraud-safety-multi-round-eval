# Python 3.10+ recommended (developed on 3.11).
#
# Core ML stack. Pin to "compatible release" rather than exact versions so this
# installs cleanly on common CPU and CUDA setups. The frozen suite was run with
# torch 2.11, torch_geometric 2.7, transformers 5.3.
torch>=2.2
torch_geometric>=2.5
transformers>=4.40

# Data / analysis
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7

# Note: the OpenAI-backed defender path uses stdlib urllib only (no openai SDK).
# Set OPENAI_API_KEY in the environment when using --model openai:<id>.
